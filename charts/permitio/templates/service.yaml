apiVersion: v1
kind: Service
metadata:
  name: {{ include "permitio.fullname" . }}
  labels:
    {{- include "permitio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "permitio.selectorLabels" . | nindent 4 }}
